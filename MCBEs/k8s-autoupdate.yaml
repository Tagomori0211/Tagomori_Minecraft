# ---------------------------------------------------------
# 1. 権限設定 (RBAC)
# ---------------------------------------------------------
apiVersion: v1
kind: ServiceAccount
metadata:
  name: minecraft-updater
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: minecraft-updater-role
  namespace: default
rules:
  # Deploymentを操作(再起動)する権限
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "patch"]
  # Podの中でコマンド実行(RCON通知)するための権限
  - apiGroups: [""]
    resources: ["pods", "pods/exec"]
    verbs: ["get", "list", "create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: minecraft-updater-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: minecraft-updater
    namespace: default
roleRef:
  kind: Role
  name: minecraft-updater-role
  apiGroup: rbac.authorization.k8s.io

---
# ---------------------------------------------------------
# 2. スクリプト本体 (ConfigMap)
# ---------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: update-script
  namespace: default
data:
  # この内容はコンテナ内の /scripts/check_and_update.sh として実体化されるよ！
  check_and_update.sh: |
    #!/bin/bash
    set -e
    
    # ターゲット設定
    DEPLOYMENT="mc-bedrock"
    CONTAINER="minecraft"
    
    echo "⏰ Daily Maintenance Job Started..."
    
    # Pod名を取得
    POD_NAME=$(kubectl get pods -l app=minecraft-bedrock -o jsonpath="{.items[0].metadata.name}")
    
    if [ -z "$POD_NAME" ]; then
      echo "❌ Minecraft pod not found. Skipping."
      exit 1
    fi

    # RCONでユーザーに通知 (いきなり落ちたら困るからね！)
   echo "📢 Sending daily restart notification..."
    # 【修正】 rcon-cli ➡️ /usr/local/bin/rcon-cli に変更 (2箇所)
    kubectl exec $POD_NAME -c $CONTAINER -- /usr/local/bin/rcon-cli say "§e[System] §bDaily Maintenance starting in 60 seconds."
    kubectl exec $POD_NAME -c $CONTAINER -- /usr/local/bin/rcon-cli say "§e[System] §fServer will restart to check for updates."
    
    sleep 60
    
    # 【修正】 ここも変更
    kubectl exec $POD_NAME -c $CONTAINER -- /usr/local/bin/rcon-cli say "§e[System] §cRebooting now..."
    # Rollout Restart
    # これを実行すると、新しいPodが作られ、その起動時にitzgイメージが
    # 自動的に最新バージョンをチェック＆ダウンロードしてくれる！
    echo "🔄 Triggering rollout restart..."
    kubectl rollout restart deployment/$DEPLOYMENT
    
    echo "✅ Maintenance restart triggered successfully."

---
# ---------------------------------------------------------
# 3. CronJob (スケジュール実行)
# ---------------------------------------------------------
apiVersion: batch/v1
kind: CronJob
metadata:
  name: minecraft-auto-updater
  namespace: default
spec:
  # 【重要】JSTを指定！これでサーバーの時刻に関わらず日本時間で動く
  timeZone: "Asia/Tokyo"
  
  # 日本時間の 朝4:00 に実行
  schedule: "0 4 * * *"
  
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: minecraft-updater
          containers:
            - name: updater
              image: bitnami/kubectl:latest
              command: ["/bin/bash", "/scripts/check_and_update.sh"]
              volumeMounts:
                - name: script-volume
                  mountPath: /scripts
          restartPolicy: OnFailure
          volumes:
            - name: script-volume
              configMap:
                name: update-script
                defaultMode: 0755 # 実行権限を付与