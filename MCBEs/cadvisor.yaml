apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: cadvisor
  namespace: default
spec:
  selector:
    matchLabels:
      app: cadvisor
  template:
    metadata:
      labels:
        app: cadvisor
    spec:
      containers:
      - name: cadvisor
        image: gcr.io/cadvisor/cadvisor:v0.47.2
        ports:
        - containerPort: 8080
          name: http
        args:
          - -housekeeping_interval=10s
          # K3sのcontainerdソケットを指定！これが超重要！
          - -containerd=/run/k3s/containerd/containerd.sock
          - -docker_only=false
        securityContext:
          # システム深部を覗くために特権を与える
          privileged: true
        volumeMounts:
        - name: rootfs
          mountPath: /rootfs
          readOnly: true
        - name: var-run
          mountPath: /var/run
          readOnly: true
        - name: sys
          mountPath: /sys
          readOnly: true
        # Docker用のマウントは削除し、K3s用を追加
        - name: k3s-containerd
          mountPath: /run/k3s/containerd
          readOnly: true
        - name: disk
          mountPath: /dev/disk
          readOnly: true
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      volumes:
      - name: rootfs
        hostPath:
          path: /
      - name: var-run
        hostPath:
          path: /var/run
      - name: sys
        hostPath:
          path: /sys
      # K3sのソケットがある場所をマウント
      - name: k3s-containerd
        hostPath:
          path: /run/k3s/containerd
      - name: disk
        hostPath:
          path: /dev/disk

---
apiVersion: v1
kind: Service
metadata:
  name: cadvisor-svc
  namespace: default
spec:
  selector:
    app: cadvisor
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30002